---
import "../styles/global.css"
const {producto} = Astro.props
---

<div class="flex flex-col justify-between gap-5 items-center bg-gray-500 p-5 rounded-2xl w-[550px]">
    <img class="w-50" src={`/img/${producto}.png`} alt="">
    <section class="w-full flex flex-wrap gap-5 justify-center">
        <slot />
    </section>
</div>